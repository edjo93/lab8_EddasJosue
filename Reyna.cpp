#include"Reyna.h"

Reyna::Reyna(){
	nombre="reyna";
}

string Reyna::getNombre(){
	return nombre;
}

bool Reyna::validarMovimiento(string coordenada,char**tablero){
	//la reina mueve en todas direcciones lo unico que se valida es que no se salga de la matriz
	//si la coordenada es valida se asigna al tablero
	//coordenadas?
	//borrar y asignar al tablero
	//tablero[i][j]=NULL;
	//tablero[if][jf]=coordenada;
}